<!DOCTYPE html>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
<%= javascript_include_tag 'jquery/jquery-3.3.1.min.js' %>
<%= javascript_include_tag 'itemList' %>
<%= stylesheet_link_tag 'itemlist' %>
<title>ECサイト</title>
</head>
<body>
	<%= render 'photos/header' %>
	<%= render 'photos/sidemenu' %>
<form method="get" action="/item/ItemDetail">
	<div class="item_list">
		<p>ITEM LIST</p>
		<div style="margin: 0 9%;">
			<!-- 要素数の為の<span> -->
			<span class="button1"></span>
			<% @items.each do |items| %>
			<button class="button1">
				<%= link_to item_ItemDetail_path :para => items.item_cd do %>
				<div class="items">
					<% if items.image.present?%>
					<p align="center"><img src="/assets/<%= items.image %>" class="itemList_itemImg" onerror="this.onerror=null;this.src='/assets/noimage.png'"></p>
					<% end %>
					<div class="top_text">ブランド:&ensp;<span><%= items.brand_name %></span></div>
					<div class="top_text">商品名:&ensp;&nbsp;&nbsp;&nbsp;<span><%= items.item_name %></span></div>
					<div class="top_text">単価:&emsp;&ensp;&ensp;&nbsp;<span>¥<%= items.price %>-</span></div>
				</div>
				<% end %> 
			</button>
			<% end %>
		</div>	
	</div>
	<div style="text-align:center;">
		<span id="prev">＜</span>　<span id="page"></span>　<span id="next">＞</span>
	</div>
</form>
<%= render 'photos/footer' %>
</body>
</html>